str = "John Doe"

# Loop through the individual character
# in a string
for ch in str:
    print(ch)

print("\n")
# Loop through the individual character
# in a string using the index of a string
for i in range(len(str)):
    print(str[i], end=" ")